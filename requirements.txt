# 전체 파이프라인(image_model/ + survey_model/ + app/) 의존성.
# 웹 앱만 실행할 경우 app/requirements.txt만 설치해도 됩니다.
#
# 아래 버전은 실제 재현성에 영향이 큰 패키지(모델 학습/pickle 직렬화·역직렬화에
# 관여)만 핀 고정했습니다. torch/torchvision/timm/scikit-learn/lightgbm/
# imbalanced-learn은 이 리포를 정리하며 실제로 학습·저장된 artifact를 다시
# 로드해 inference까지 성공한 버전입니다. optuna는 프로젝트에서 실제 사용한
# conda 환경(skin)에 설치된 버전을 기준으로 핀 고정했으나, 이번 정리 과정에서
# 이미지 데이터 부재로 03_image_tune_optuna.py 자체를 재실행해 확인하지는
# 못했습니다. imagehash는 01b_dedup_split.py를 실행했던 환경에 실제
# 설치된 버전으로 핀 고정했습니다.
torch==2.5.1
torchvision==0.20.1
timm==1.0.26
scikit-learn==1.7.2
lightgbm==4.6.0
imbalanced-learn==0.14.0
optuna==4.8.0
imagehash==4.3.2
pandas>=1.5.0
numpy>=1.23.0
pillow>=9.0.0
matplotlib>=3.6.0
seaborn>=0.12.0
scipy>=1.10.0
statsmodels>=0.14.0
tqdm>=4.65.0
streamlit>=1.32.0
bcrypt>=4.0.0
